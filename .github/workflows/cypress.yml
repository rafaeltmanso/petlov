name: Cypress E2E Tests Multi-Browser
on:
  workflow_dispatch:
    inputs:
      spec:
        default: cypress/e2e/*
    
jobs:
  chrome-tests:
    runs-on: ubuntu-latest
    container:
      image: cypress/browsers:22.16.0 # Uses Cypress Docker image with Chrome and Firefox pre-installed
      options: --user root # Runs as root user to avoid permission issues
    # Runs tests in parallel with matrix strategy https://docs.cypress.io/guides/guides/parallelization
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2] # Uses 2 parallel instances
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        
      - name: Instalar dependências
        run: npm install
        
      - name: Executar testes no Chrome
        uses: cypress-io/github-action@v6
        with:
          install: false # Skip install since we already installed dependencies
          wait-on: 'https://petlov.vercel.app'
          browser: chrome
          record: true
          parallel: true
          group: "Chrome" # Groups results by browser in Cypress Cloud
          spec: ${{ github.event.inputs.spec }}
        env:
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  firefox-tests:
    runs-on: ubuntu-latest
    container:
      image: cypress/browsers:22.16.0 # Uses Cypress Docker image with Chrome and Firefox pre-installed
      options: --user root --shm-size=2g # Runs as root user and increases shared memory for Firefox
    # Runs tests in parallel with matrix strategy https://docs.cypress.io/guides/guides/parallelization
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2] # Uses 2 parallel instances
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        
      - name: Instalar dependências
        run: npm install
        
      - name: Executar testes no Firefox
        uses: cypress-io/github-action@v6
        with:
          install: false # Skip install since we already installed dependencies
          wait-on: 'https://petlov.vercel.app'
          browser: firefox
          record: true
          parallel: true
          group: "Firefox" # Groups results by browser in Cypress Cloud
          spec: ${{ github.event.inputs.spec }}
        env:
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Firefox-specific environment variables for Docker containers
          MOZ_CRASHREPORTER_DISABLE: 1
          MOZ_HEADLESS: 1

  electron-tests:
    runs-on: ubuntu-latest
    container:
      image: cypress/browsers:22.16.0 # Uses Cypress Docker image with Chrome and Firefox pre-installed
      options: --user root # Runs as root user to avoid permission issues
    # Runs tests in parallel with matrix strategy https://docs.cypress.io/guides/guides/parallelization
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2] # Uses 2 parallel instances
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        
      - name: Instalar dependências
        run: npm install
        
      - name: Executar testes no Electron
        uses: cypress-io/github-action@v6
        with:
          install: false # Skip install since we already installed dependencies
          wait-on: 'https://petlov.vercel.app'
          browser: electron
          record: true
          parallel: true
          group: "Electron" # Groups results by browser in Cypress Cloud
          spec: ${{ github.event.inputs.spec }}
        env:
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}